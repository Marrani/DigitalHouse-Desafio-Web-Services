<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.gabrielmarrani.marvel">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".detalhe.view.ImagemDetalheActivity"></activity>
        <activity android:name=".detalhe.view.DetalheActivity"  />
        <activity android:name=".comics.view.HomeActivity" />
        <activity
            android:name=".register.view.RegisterActivity"
            android:label="Register"
            android:parentActivityName=".login.view.LoginActivity"
            android:theme="@style/Register" />
        <activity android:name=".login.view.LoginActivity" />
        <activity
            android:name=".Splashscreen.view.SplashScreenActivity"
            android:theme="@style/Splash">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>